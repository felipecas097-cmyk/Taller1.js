// Pedir al usuario que ingrese una frase
let frase = prompt("Ingresa una frase:");

// Usar un ciclo for para repetir 10 veces
for (let i = 0; i < 10; i++) {
    // Imprimir la frase en la consola cada vez que se repite el ciclo
    console.log(frase);
}